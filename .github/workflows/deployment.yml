name: Event Demo 1
on: 
 push:
  branches:
    - main
    
    
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:

      - name: Get Code
        uses: actions/checkout@v4
      - name: Install dependancy
        run: npm ci
      - name: Lint code
        run: npm run lint
      - name: Test code
        run: npm run test
  build:
    needs: deploy
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v4
      - name: Install dependancy
        run: npm ci
      - name: Building website
        run:  npm run build
      - name: updald atrifact
        uses: actions/upload-artifacts@v4
        with:
          name: dist-files
          path: |
            dist
            package.json
  deplo:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy
        run: echo "Deploying...."

   



  